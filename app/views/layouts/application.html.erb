<!DOCTYPE html>
<html>
<head>
  <title>Maratonafga</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav>
  <div class="nav-wrapper black">
    <img class="left" src="<%= asset_path('unb-logo.jpg') %>" alt="" style="width: 114px; height: 64px; padding-left: 30px;">
    <ul id="nav-mobile" class="left hide-on-med-and-down">
    	<li style="font-size: xx-large; padding-left: 10px;">Maratona UnB</li>  
    </ul>
    <ul id="nav-mobile" class="right hide-on-med-and-down">
      <% if user_signed_in? %>
        <%= link_to("Hello " + current_user.email + ", Logout", destroy_user_session_path, :method => :delete) %>
      <% else %>
        <%= link_to('Login', new_user_session_path)  %>  
      <% end %>
      <li><a href="sass.html">About</a></li>
    </ul>
  </div>
</nav>

<%= yield %>

</body>
</html>
